<div id="searchcontent">
<h2 id="searchtitle">Title Ordered</h2>
<%= form_tag stepsixes_path, :method => 'get' do%>
    <%= hidden_field_tag :direction, params[:direction]%>
    <%= hidden_field_tag :sort, params[:sort]%>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
    </p>
<% end %>
<table class="search">
  <tr>
    <th><span><%= sortable "customer_id", "Customer #"%>&nbsp;</span></th>
    <th><span><%= sortable "customer_lname", "Last" %>, <%= sortable "customer_fname", "First" %>&nbsp;</span></th>
    <th><span><%= sortable "title", "Step Status" %>&nbsp;</span></th>
    <th><span><%= sortable "updated_at", "Updated at" %>&nbsp;</span></th>
    <th>Modify</th>
    <th>Remove Step</th>
  </tr>
<tbody>
<% @stepsixes.each do |stepsix| %>
  <tr>
    <td><%= stepsix.customer_id %></td>
    <td><%= stepsix.customer_lname.capitalize %>, <%= stepsix.customer_fname.capitalize %></td>
    <td><%= stepsix.title %></td>
    <td><%= stepsix.updated_at %></td>
    <td><%= link_to 'Edit', edit_stepsix_path(stepsix) %></td>
    <td><%= link_to 'Destroy', stepsix, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</tbody>
</table>
<%= will_paginate @stepsixes %>

<br />
</div>