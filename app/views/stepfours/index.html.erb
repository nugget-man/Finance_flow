<div id="searchcontent">
<h2 id="searchtitle">GFE Received</h2>
<%= form_tag stepfours_path, :method => 'get' do%>
    <%= hidden_field_tag :direction, params[:direction]%>
    <%= hidden_field_tag :sort, params[:sort]%>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
    </p>
<% end %>
<table class="search">
  <tr>
    <th><span><%= sortable "customer_id", "Customer #"%>&nbsp;</span></th>
    <th><span><%= sortable "customer_lname", "Last" %>, <%= sortable "customer_fname", "First" %>&nbsp;</span></th>
    <th><span><%= sortable "title", "Step Status" %>&nbsp;</span></th>
    <th><span><%= sortable "updated_at", "Updated at" %>&nbsp;</span></th>
    <th>Modify</th>
    <th>Remove Step</th>
  </tr>
<tbody>
<% @stepfours.each do |stepfour| %>
  <tr>
    <td><%= stepfour.customer_id %></td>
    <td><%= stepfour.customer_lname.capitalize %>, <%= stepfour.customer_fname.capitalize %></td>
    <td><%= stepfour.title %></td>
    <td><%= stepfour.updated_at %></td>
    <td><%= link_to 'Edit', edit_stepfour_path(stepfour) %></td>
    <td><%= link_to 'Destroy', stepfour, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</tbody>
</table>
<%= will_paginate @stepfours %>

<br />
</div>