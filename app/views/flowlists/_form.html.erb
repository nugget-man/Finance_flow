<%= form_for(@flowlist) do |f| %>
  <% if @flowlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flowlist.errors.count, "error") %> prohibited this flowlist from being saved:</h2>

      <ul>
      <% @flowlist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
