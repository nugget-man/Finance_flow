

<table style="margin-left: auto;margin-right: auto;text-align: left;"><tr>
  <td>
<p id="notice"><%= notice %></p>

<p>
  <b>Customer:</b>
  <%= @stepthirteen.customer_id %>
</p>

<p>
  <b>Customer fname:</b>
  <%= @stepthirteen.customer_fname %>
</p>

<p>
  <b>Customer lname:</b>
  <%= @stepthirteen.customer_lname %>
</p>

<p>
  <b>Complete:</b>
  <%= @stepthirteen.complete %>
</p></td>
<td>


<p>Update Customer Status</p>
    <% form_tag details_path(@customer.id), :method => 'put' do%>
        <%= text_field_tag "details"%>
        <%= submit_tag "Save", :name => nil%>
    <% end %>
    <!-- Comments Box -->
    <%= form_tag "/customers/add_new_comment" do %>
        <%= hidden_field_tag "id", @customer.id %>
        <%= hidden_field_tag "comment[commentable_id]", @customer.id %>
        <%= hidden_field_tag "comment[title]", "Final Underwriting" %>
        <%= hidden_field_tag "comment[user_id]", current_user.id %>
        <%= text_area_tag "comment[comment]" %><br>
        <%= submit_tag "Post Comment" %>
    <% end %><br>
  <div class="commentshow">
<% if @comments%>
    <%= render :partial => 'comment_output', :comments => @comments %>
<% end %></div>
    </td>
</tr></table>
<%= link_to 'Edit', step14edit_path(params[:id])%> |
<%= link_to 'Back', root_path %>