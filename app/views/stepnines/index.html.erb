<div id="searchcontent">
<h2 id="searchtitle">Check in HUD</h2>
<%= form_tag stepnines_path, :method => 'get' do%>
    <%= hidden_field_tag :direction, params[:direction]%>
    <%= hidden_field_tag :sort, params[:sort]%>
    <p>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
    </p>
<% end %>
<table class="search">
  <tr>
    <th><span><%= sortable "customer_id", "Customer #"%>&nbsp;</span></th>
    <th><span><%= sortable "customer_lname", "Last" %>, <%= sortable "customer_fname", "First" %>&nbsp;</span></th>
    <th><span><%= sortable "title", "Step Status" %>&nbsp;</span></th>
    <th><span><%= sortable "updated_at", "Updated at" %>&nbsp;</span></th>
    <th>Modify</th>
    <th>Remove Step</th>
  </tr>
<tbody>
<% @stepnines.each do |stepnine| %>
  <tr>
    <td><%= stepnine.customer_id %></td>
    <td><%= stepnine.customer_lname.capitalize %>, <%= stepnine.customer_fname.capitalize %></td>
    <td><%= stepnine.title %></td>
    <td><%= stepnine.updated_at %></td>
    <td><%= link_to 'Edit', edit_stepnine_path(stepnine) %></td>
    <td><%= link_to 'Destroy', stepfour, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</tbody>
</table>
<%= will_paginate @stepnines %>

<br />
</div>

